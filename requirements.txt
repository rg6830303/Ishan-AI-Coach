streamlit>=1.28.0
openai>=1.6.0
faiss-cpu>=1.7.4
sentence-transformers>=2.2.2
bcrypt>=4.1.0
python-dotenv>=1.0.0
numpy>=1.24.0
rank-bm25>=0.2.2
# Browser-based voice input (mic capture + speech-to-text). Optional: the app
# degrades gracefully to text-only if this is unavailable.
streamlit-mic-recorder>=0.0.8
